{
  "schemaVersion": "2.4",
  "id": "example_channel",
  "name": "Example Channel",
  "description": "Enhanced photo display channel with dynamic asset discovery",
  "version": "2.0.0",
  "update_schedule": {
    "unit": "hours",
    "duration": 1
  },
  "permissions": ["read:photos"],
  "settings_type": "simple",
  "settings": {
    "image_choice": {
      "type": "select",
      "enum": ["image1", "image2", "image3"],
      "label": "Image to Display",
      "default": "image1",
      "description": "Select which image to display from assets"
    },
    "update_interval_unit": {
      "type": "select",
      "enum": ["days", "hours", "minutes", "seconds"],
      "label": "Update Interval Unit",
      "default": "minutes"
    },
    "update_interval_value": {
      "type": "integer",
      "minimum": 1,
      "label": "Update Interval Value",
      "default": 5,
      "description": "How often to check for updates"
    }
  }
}

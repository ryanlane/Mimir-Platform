// Archived: Distribution dashboard has been migrated to a minimal stats bar on the main Dashboard.
// This stub remains temporarily so imports (if any lingering) do not break builds.
export default function Distribution() {
  return (
    <div style={{padding: '2rem'}}>
      <h1>Distribution Dashboard Archived</h1>
      <p>The full distribution dashboard has been archived. Core metrics now appear on the main Dashboard.</p>
      <p>If you need the original functionality, restore it from <code>src/_archived/distribution</code>.</p>
    </div>
  );
}
